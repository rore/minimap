{
  "name": "minimap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node package/minimap/server.js",
    "test": "node --test",
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "sync-mirrors": "node scripts/sync-mirrors.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
